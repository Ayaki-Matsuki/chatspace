.chat-body__header
  .chat-body__name
    = message.user.name
  .chat-body__time
    = message.created_at.strftime("%Y/%m/%d %H:%M")
.chat-body__message
  - if message.content.present?
    = message.content
  = image_tag message.image.url, class: 'chat-body__message__image' if message.image.present?
